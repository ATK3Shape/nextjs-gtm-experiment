import Head from 'next/head'
import { useState } from 'react'
// import Image from 'next/image'
// import { Inter } from 'next/font/google'
import styles from '../styles/Home.module.css'
import Modal from 'react-modal';

// const inter = Inter({ subsets: ['latin'] })
const customStyles = {
  content: {
    top: '50%',
    left: '50%',
    right: 'auto',
    bottom: 'auto',
    marginRight: '-50%',
    transform: 'translate(-50%, -50%)',
    height: '70%',
    width: '70%',
  },
};

export default function Home() {
  const [isOpen, setIsOpen] = useState(false)

  const openModal = () => {
    console.log('open')
    setIsOpen(true);
  }

  function closeModal() {
    setIsOpen(false);
  }

  return (
    <>
      <Head>
        <title>Home</title>
        <meta name="description" content="Generated by create next app" />
        <meta name="viewport" content="width=device-width, initial-scale=1" />
        <link rel="icon" href="/favicon.ico" />
      </Head>
      <main className={styles.main}>
        <div className={styles.description}>
          <p>Welcome to m GTM test website!</p>
        </div>

        <div className={styles.grid}>
          <div className={styles.productCard}>
            <h2>Beosound A9</h2>
            <p>Don`&apos;`t mess with the classics. Unless it`&apos;`s to bring them bang up to date. Beosound A9 is now available with stereo pairing, smoother in-app setup, and improved Wi-Fi performance.</p>
            <a href="/about">Read more</a>
          </div>
          <div className={styles.productCard}>
            <h2>BEOPLAY H95</h2>
            <p>Moving, masterful, mesmerising. Get lost in your music with adjustable noise-cancelling headphones that redefine grab-and-go listening.</p>
          </div>
          <div className={styles.productCard}>
            <h2>Oak Wood - 77</h2>
            <p>Go for a refined look with handcrafted oak wood and natural aluminium.</p>
            <button onClick={openModal}>Open window</button>
          </div>
        </div>

        <Modal
          isOpen={isOpen}
          onRequestClose={closeModal}
          style={customStyles}
          contentLabel="Example Modal"
        >
          <button onClick={closeModal}>close</button>
          <h2>Hello</h2>
          <div>I am a modal</div>
        </Modal>
      </main>
    </>
  )
}
